Source: ncid-0.67.tar.gz
Fedora Core 5 package: ncid-0.67-1.i386.rpm
TiVo Series1 package: ncid-0.67-series1-tivo.tgz
TiVo Series2 package: ncid-0.67-series2-tivo.tgz
Macintosh OS X package: ncid-0.67-mac-osx.tgz
FreeBSD 5.4 package: ncid-0.67-freebsd.tgz
Cygwin Windows package: ncid-0.67-cygwin.tgz
Windows client installer: ncid-0.67-setup.exe

Server Changes:

    None

Client Changes:

    Moved a set line so its last in ncid.conf. This fixes a TiVo problem
    that caused ncid to abort.

    Created new option for ncidsip to detect SIP packets on the network
    without setting up the ncidd server.  If SIP packets exist, ncidsip
    will display the Caller ID and CANCAL lines if a call comes in.

    Test for SIP packets on port 5061 (must be root):

        ncidsip --test

    Test for SIP packets on port 10,000 (must be root):

        ncidsip --test --sip :10000

    Created new option for ncidsip to detect all network device names:

        ncidsip --listdevs


Client Module Changes:

    None

Script changes:

    None

Documentation Changes:

    Updated the ncidsip.8 man page to include the test and usage options.

    Updated the setup for ncidsip in the Setup-VOIP file.
