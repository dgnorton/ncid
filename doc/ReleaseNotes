Source: ncid-0.60.tar.gz
Fedora Core 4 package: ncid-0.60-1.i386.rpm
TiVo Series1 package: ncid-0.60.series1-tivo.tgz
TiVo Series2 package: ncid-0.60.series2-tivo.tgz
Macintosh OS X package: ncid-0.60.mac-osx.tgz
FreeBSD package: ncid-0.60.freebsd.tgz
Windows client package: ncid-0.60.zip

NOTE:
    - ncidd requires the new ncidd.conf, the previous version will not work.
    - ncidd requires aliase definitions inside ncidd.alias
    - requires NCIDpop version 0.9.9.10 or later

Added support for the NetCallerID device.

Added support for Distinctive Ring

Added three more config files: ncid.conf, ncidscript.conf, and ncidrotate.conf

Removed aliases from ncidd.conf and put them into ncidd.alias.
The ncidd.conf file will not accept aliases anymore.

Changed the way ncidd discovers a modem so it will work with slow
responding ones.

Changed some ncidd options and key words in the configuration file.

A log file name has been changed: cidmodem.log is now ciddata.log
